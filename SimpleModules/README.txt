export GCC_13_PATH=/home/andtokm/DiskS/Utils/bin/gcc-13.1
export PATH=${GCC_13_PATH}/bin:${PATH}

make

export LD_LIBRARY_PATH=/home/andtokm/DiskS/Utils/bin/gcc-13.1/lib64

=================================================================================================================================
                                modinfo testmod.ko
=================================================================================================================================

filename:       /home/andtokm/DiskS/ProjectsUbuntu/CppMakeProject/LinuxModules/testmod.ko
license:        GPL
description:    Hello world driver
author:         Andrei Tokmakov
srcversion:     4276224FEF081F5120DDBF1
depends:        
retpoline:      Y
name:           testmod
vermagic:       5.15.0-76-generic SMP mod_unload modversions 


=================================================================================================================================

=================================================================================================================================

# На этом этапе команда:

	sudo lsmod | grep testmod

# Загрузить модуль:

	sudo insmod testmod.ko

# 	
	sudo lsmod | grep testmod

	#  testmod                16384  0
	

# Удалить модуль:

	sudo rmmod testmod

=================================================================================================================================

=================================================================================================================================

sudo journalctl --since "1 hour ago" | grep kernel

=================================================================================================================================
						# Модули ядра должны иметь не менее двух функций:
=================================================================================================================================

init_module()     вызывается при внедрении (insmod) модуля в ядро;
cleanup_module()  вызывается непосредственно перед извлечением модуля из ядра.